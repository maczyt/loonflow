import { Box } from '@mui/system';

const FieldTag = () => {
  return (
    <Box
      sx={{
        background: '#f7f8fa',
        borderRadius: '2px',
      }}
    ></Box>
  );
};

export default FieldTag;
